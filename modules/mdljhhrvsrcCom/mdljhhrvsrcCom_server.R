# 
# mdlDemoServer::buttonServer(input = input,output = output,session = session,dms_token = dms_token)
# 
# #完成日期组件注册
# mdlDemoServer::dateServer(input = input,output = output,session = session,dms_token = dms_token)


#jhhrv
# mdljhhrvServer::HrvServer(input = input,output = output,session = session,dms_token = dms_token)

#jhhrv
mdljhhrvsrcServer::jhhrvsalaryServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvsocialsecurityServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvdetailServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvrddetailServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvnonrddetailServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvrulevoucherServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvtplvoucherServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvtploutputvoucherServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvfnoteServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvacctServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvdeptServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvacctreclassServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvworkcenterServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvrditemServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvcosucompanyServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvmiddleTableServer(input = input,output = output,session = session,dms_token = dms_token)

mdljhhrvsrcServer::jhhrvdeptcomparisonServer(input = input,output = output,session = session,dms_token = dms_token)


  

